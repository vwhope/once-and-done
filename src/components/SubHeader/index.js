import React from "react";
import "./style.css";

function SubHeader(props) {
    return (
        <div className="subbackground">
            <p className="subline">Click each image, but ONLY ONCE or the game is over!</p>
        </div>
    );
}

export default SubHeader;